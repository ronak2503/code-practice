#include <stdio.h>
#include <string.h>
int main()
{
    char st[10];
    printf("enter you name\n");
    gets(st); 
    strlen(st);
    printf("the lenght of string st is %d",strlen(st));
    return 0;
}

/*

    // yashw#$#%#$5l ;kasdwasht sahu4 yashawna#4534
    //  ACII use symbole rang in ACII   
    // if and else how to will you manage it ?
    // chale jaao yha se aap Jan Addhaar laaoo
                                   HINT
                s1 = break the string into array of words
                s2 = traverse all word char by char to read its assci 
                s3 = Check out it's len at the same time

*/
